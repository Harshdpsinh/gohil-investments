node_modules/
dist/
.env
.env.local
.DS_Store
*.log
*token*.txt
*uid*.txt
node-*-x64.msi
openwa/
evolution-api/

# Credentials — never commit. Listing a file here does NOT untrack it if it was
# already added; use `git rm --cached <file>` for that.
FIREBASE.txt
*.zip

